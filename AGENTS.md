# AGENTS.md

For non-trivial tasks:
- explain the goal in simple language first
- work in the smallest useful step
- make one narrow change at a time
- avoid large rewrites unless requested
- show how to verify the result

Keep explanations brief unless the user asks for more detail.

Project path rule:
- The current project root is `D:\00 博士阶段\99 Project\06 EA`.
- The current project memory root is `D:\00 博士阶段\99 Project\06 EA\project_memory`.
- `C:\Users\admin\Documents\New project` is only a legacy Junction alias to the D drive project root.
- Do not write, delete, or report project memory through the C drive alias.
- When updating project memory, use the D drive absolute path explicitly and do not rely on `./project_memory` from the current working directory.
