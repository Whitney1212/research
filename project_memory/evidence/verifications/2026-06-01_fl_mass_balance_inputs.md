# 2026-06-01 FL 质量守恒修正计算输入入口

## 本次新增信息

- 用户明确补充：Flares 的移动位置数据文件为 `D:\00 博士阶段\博一\05 Project\com_260326\20250313_20250419.xlsx`。该位置数据中，数值越接近 `0`，表示越靠近 `MT` 方向，也就是轨道南端；`Flares = FL = 移动平台`。 [来源: 用户当前对话 2026-06-01] [已核验: `D:\00 博士阶段\博一\05 Project\com_260326\20250313_20250419.xlsx`]
- 用户指出，后续若要复用既有代码，可优先检查 `D:\00 博士阶段\博一\05 Project\com_260326\compute\com_260326` 和 `D:\00 博士阶段\博一\05 Project\com_260507\COMPUTE\EA_com`。前者当前包含 `cut&check.R`、`geo_trans.R`、`night.R`、`select_use.R`、`track_trans.R` 等与移动平台轨迹或数据筛选相关的脚本。 [来源: 用户当前对话 2026-06-01] [已核验: `D:\00 博士阶段\博一\05 Project\com_260326\compute\com_260326`]
- 用户指出，四个典型天的关键数据和信息集中在 `D:\00 博士阶段\博一\05 Project\com_260507`。该目录当前包含 `DATA`、`COMPUTE`、`OUTPUT` 以及多个 metadata 文件；其中 `COMPUTE\EA_com` 是当前 EA/raw-w 结果主目录。 [来源: 用户当前对话 2026-06-01] [已核验: `D:\00 博士阶段\博一\05 Project\com_260507`] [已核验: `D:\00 博士阶段\博一\05 Project\com_260507\COMPUTE\EA_com`]

## 对后续计算的约束

- 对 `D:\00EDDYPRO\com_260401\Flares` 下四天 FL 高频数据做质量守恒修正时，不能只按固定 30 min 窗口解释为空间切面。若计算目标是沿原 `Fc_adv_2025.R` 思路的移动单程或空间段质量守恒修正，应先用上述位置 Excel 或既有轨迹脚本确定移动平台在每个时段的空间位置和方向。
- `lambda` 修正的核心口径仍是按窗口或移动段内原始 `w` 的正负空气量平衡：若同时有上升和下沉样本，则 `lambda = -sum(w[w > 0]) / sum(w[w < 0])`，并将 `w < 0` 的样本乘以 `lambda`；若 `w` 全正或全负，则 `lambda = 1`，但应标记为无法真正完成上下空气量平衡。
- 若本轮只计算“质量守恒修正情况”，最小输出应优先包括每个窗口或移动段的 `lambda`、正负样本数、正负 `w` 总量、修正后净 `w` 总量、异常标记和覆盖率；是否进一步计算 CO2 平流通量应作为下一步单独确认。
